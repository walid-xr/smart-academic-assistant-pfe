{
  "name": "smart-academic-assistant",
  "version": "1.0.0",
  "private": true,
  "description": "Root launcher for the Smart Academic Assistant full-stack project",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:no-n8n": "node scripts/dev.js --no-n8n",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "test:structured-smoke": "npm run test:structured-smoke --prefix backend"
  },
  "devDependencies": {
    "pptxgenjs": "^4.0.1"
  }
}
